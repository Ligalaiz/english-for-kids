import {get} from "../storage.utils";

export default function safeProgressUtils(cardName, type) {
  // const gameProgressData = get('gameProgress');

  // let trainCard = gameProgressData.find((item) => item.cardName === cardName);
  // if (trainCard) {
  //   trainCard.type = type;
  //   set('gameProgress',)
  // } else {
  //   trainCard.type = type;
  // }

}